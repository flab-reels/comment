version: 0.2
env:
  variables:
    SPRING_PROFILE: "prod"
phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
#      - echo Logging in to Amazon ECR...
#      - aws --version
#      - aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 087334185325.dkr.ecr.ap-northeast-2.amazonaws.com
#      - REPOSITORY_URI=087334185325.dkr.ecr.ap-northeast-2.amazonaws.com/docker-demo
#      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
#      - IMAGE_TAG=build-$(echo $CODEBUILD_BUILD_ID | awk -F":" '{print $2}')
  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...
      - docker build -t no-cache-build .
  post_build:
    commands:
#      - mv build-output/**/* ./
#      - mv build-output/* ./
#      - rm -R build-output *.yml LICENSE README* .git*
#      - echo Build completed on `date`
#      - echo Pushing the Docker images...
#      - docker push $REPOSITORY_URI:latest
#      - docker push $REPOSITORY_URI:$IMAGE_TAG
#      - echo Writing image definitions file...
#      - printf '[{"name":"order-service","imageUri":"%s"}]' $REPOSITORY_URI:$IMAGE_TAG > imagedefinitions.json
#      - cat imagedefinitions.json

artifacts:
  files:
    -  '**/*'
#    - imagedefinitions.json